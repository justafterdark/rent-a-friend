<div class="container">
  <%= simple_form_for [@event] do |e| %>
    <%= e.input :datetime %>
    <!-- 'event[category]' formats the data into a key value pair (eg. Hash) -->
    <!-- 'form-control' is a bootstrap class to beautify the dropdown -->
    <select name="event[category]" class="form-control" id="">
      <% @categories.each do |c| %>
        <option value="<%= c %>" <%= 'selected' if @type == c %>> <%= c %></option>
      <% end %>
    </select>
    <%= e.input :description %>
    <%= e.input :event_location %>
    <%= e.input :meeting_location %>
    <% if @event.id.present? %>
      <%= e.input :confirmed %>
    <% end %>
    <%= e.input :public, label: 'Public (list event to rent a crowd)' %>
    <%= e.submit "Create", class: "btn btn-primary" %>
  <% end %>
</div>
